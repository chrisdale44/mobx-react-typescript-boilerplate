/*
 *  Container for constants
 */
var redirect = window.location.origin + window.location.pathname;

const PIN = {
    APP: '4856836359995802992', //4855819497409359779
    FIELDS: 'id,name,image[small]',
    SCOPE: 'read_public, write_public'
};

export default PIN;
